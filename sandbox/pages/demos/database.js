import DbFetcher from '../../components/database'
import Template from '../../components/template'

export default () => {
   return (
      <Template title="Database Query Demo">
         <DbFetcher /> 
      </Template>
   );
}